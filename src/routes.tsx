import { createBrowserRouter } from "react-router-dom";

import Home from "./pages/home";

export default createBrowserRouter([
  {
    element: <Home />,
    path: "/react-fem-results-summary-component",
  },
]);
